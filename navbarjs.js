/* 
let toggleButton = document.getElementById('check_input_menu')[0]


let navbarLinks = document.getElementBy('navbar-links')

toggleButton.addEventListener('click', () => {
    navbarLinks.classList.toggleButton('active')
}) */